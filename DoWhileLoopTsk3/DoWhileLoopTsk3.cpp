#include "stdafx.h"
#include "conio.h"
#include <string>
#include <cstdlib>
#include <iostream>

using namespace std;

/*Using other things such as CASE and IF
Create a menu system that allows a user to register with the program or login
They should not be given the option to login until they have registered with the system
The same login rules apply after they have registered with the system
When completed, a password recovery option should be added 
This should have a secret question attached to it*/


int main()
{
    return 0;
}

